#Voice Agent Boilerplate

This repo serves as a boilerplate for a dialogflow voice agent for the Google Assisant.

##Heroku deploy
``` Heroku logs --tail ```

Google Cloud needs two environment variables:

```  export GCLOUD_PROJECT=??????```
